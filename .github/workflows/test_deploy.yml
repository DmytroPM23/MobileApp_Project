name: CI Test & Deploy (Demo)

on:
  push:
    branches:
      - actions_test_student6

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run fake unit tests
        run: echo "✅ All tests passed!"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Fake deploy
        run: echo "✅ App deployed successfully!"
